# 把开源插件改造成「多租户平台可用」

> **English (primary): [PLUGIN-PORTING.md](PLUGIN-PORTING.md)** ｜ **[中文文档](PLUGIN-PORTING.zh-CN.md)（当前）**

> 面向 **DSH 插件作者**与**自建托管平台的维护者**。
> 讲清一件事：**为什么很多插件在本地跑得好好的，一放进托管平台就废**，以及**具体怎么改**。
> 全文以真实案例 **`dsh-univer-office`**（一个 41 MB 的在线表格插件）为样本 —— 它完整走过了「静态预检通过 → 平台上完全不可用 → **六轮后续改造** → 通过回归」的全过程。**第一次修完通常不是终点**，§4.5 逐轮列出了全部六轮。

---

## 0. 先看结论

| 现象 | 根因类别 | 改法 | 能否靠平台配置绕过 |
|---|---|---|---|
| 插件界面能打开，但数据/图表加载不出来 | 交付给浏览器的地址是**绝对 URL** | 改成**同域相对路径** | ❌ 不能，必须改插件 |
| 插件内部服务起不来 / 健康检查超时 | 内部通信走 **TCP loopback** | 改 **unix domain socket** 或 stdio | ❌ 不能（隔离层就是为此设计的） |
| 装上插件后实例**反复崩溃重启** | 与平台内置 `@deepseek-ai/*` **版本不兼容** | 对齐依赖范围 / 导出符号 | ❌ 不能 |
| 一个插件吃掉 1/6 的实例内存 | **重型依赖在入口处静态 import** | 改 `await import()` **懒加载** | ❌ 不能 |
| 多租户下互相干扰 | **自建监听端口** | 复用 Host 的唯一入口 | ❌ 不能 |
| 操作**报了错，效果却真的发生了**（或内部服务在请求中途死掉） | **原生绑定要求的 glibc 比宿主新** | 自带一份 **glibc 运行时**，让该进程经它启动 | ❌ 不能，必须改插件（或换基底） |
| 插件「装上了」但界面永远不出现 | `dsh.client.inject` 里列了**该角色不下发的 UI 包** | 从 `inject` 里移除 | ❌ 不能，必须改插件 |

> **静态预检全绿 ≠ 到站。** 这个插件预检全过，首轮改造之后仍然做了**六轮**（0.2.24 → 0.2.29）：一个把插件自己的 socket 垫片静默废掉的打包顺序缺陷；一个要求宿主 glibc 更新的原生绑定；**第二个进程**里的崩溃，表现为**写入报假错**；一条永远无法满足的客户端 `inject`；自带 glibc 运行时，解开了原生 Office 导入导出；最后是**在平台升级之前把 peer 范围放宽**。§4.5 逐轮列出。

**一句话判据**：

> **插件的一切对外交互，是否都能走「平台已经有的那一条入口」？**
> 即 —— **浏览器侧只用相对路径；实例侧不依赖本机 loopback 上的网络服务。**

---

## 1. 托管平台和「本地跑」差在哪

插件开发者本地的假设，在托管平台上**全部不成立**：

| 本地假设 | 托管平台的实际 |
|---|---|
| 「`127.0.0.1` 就是我这台机器」 | 服务端 `127.0.0.1` = **服务器**；浏览器里的 `127.0.0.1` = **用户自己的电脑** —— 两个不同的地方 |
| 「网页和数据服务在同一台机器，随便连」 | 实例进程 uid 被**出网护栏**限制，连自己拉起的子进程都可能连不上（见 §4.1） |
| 「端口随便挑，冲突了换一个」 | 多租户**共主机**：端口是共享资源，还可能有端口漂移与跨租户可达风险 |
| 「HTTP 就行」 | 用户访问的是 **HTTPS 域名**；HTTPS 页面里嵌 HTTP 资源会被浏览器**硬拦**（混合内容） |
| 「进程是我的，内存随便用」 | 每用户实例有**内存配额**，一个插件就能把整实例压垮 |
| 「装一个插件就是一个目录」 | 平台会**预检、启停、隔离、回滚**插件；不兼容会拖垮实例 |
| 「宿主什么依赖都有」 | 基底镜像是**固定的**；预编译的原生扩展可能要**更新的系统库**（见 §2 H8） |

---

## 2. 八个失败模式（上架前先自查）

### H1 · 硬编码 loopback 服务地址

```js
// ❌ 实例内的服务地址被写死
const gateway = `http://127.0.0.1:${port}`
```

**症状**：宿主进程连不上自己 `spawn` 出来的子进程（健康检查恒超时）。
**为什么**：托管平台把实例 uid 段到 `127.0.0.0/8` 的**主动连接**拒掉了（只拒主动发起，回包不受影响 —— 否则实例整体不可用）。
**等级**：🟡 警告（若仅内部使用，可改 socket；若还要交给浏览器 → 升为 🔴）

### H2 · client bundle 里出现绝对 URL 🔴 **阻断级**

```js
// ❌ 这个 URL 会被塞进 iframe src，让**用户的浏览器**去连
viewerUrl = `${gateway}/?file=${fileKey}`
```

**症状**：页面框架能渲染，但内容永远白屏 / 一直在转圈。
**为什么**：URL 一旦交给浏览器，就**必然失效**：跨机器（`127.0.0.1` 指向用户电脑）＋ 混合内容被拦。
**关键**：**平台侧无任何补救手段** —— 不能靠配置、不能靠反向代理、更不能靠"换成服务器 IP"（见 §4.2）。
**判据**：只扫 **client 侧产物**；命中 `http://` 字面量即视为「会交给浏览器」→ **拒绝投放**。

### H3 · 自建网络监听

扫描 `listen(` / `createServer` 的实参形态：
- 监听 unix socket / stdio → ✅ 好
- 监听 loopback TCP → 🟡 需人工确认是否为唯一入口（若是，按 H1/H2 处理）

### H4 · 浏览器地址由客户端自己拼

服务端返回**路径**（相对），客户端拼成绝对地址 —— 同样中招。
**判据**：交付浏览器的地址**必须由 Host 生成且为相对路径**。

### H5 · 与平台内置包不兼容 → 崩溃循环

托管平台把 DSH 相关包**内置**在固定位置。插件声明/引用它们时必须同时满足两条：

| 判据 | 说明 |
|---|---|
| **A. 依赖范围**（semver，**默认语义**，不传 `includePrerelease`） | 插件声明的 `@deepseek-ai/*` 范围必须**接受**平台内置版本。写 `>=0.1.1-rc.1 <0.1.2` 而平台是 `0.1.2-rc.1` ⇒ 不满足 ⇒ 包管理器给插件装**自带**的旧版 → 与平台包冲突 |
| **B. 导出符号**（运行时真值） | 插件 `import { X } from '@deepseek-ai/Y'` 时，`X` 必须在平台包 Y 的**运行时真实导出**里存在。取法 = `await import()` 后看 `Object.keys()`，比解析 `.d.ts` 可靠 |

**为什么必须用默认语义**：包管理器的实际安装判定就是默认语义。传 `includePrerelease` 会把这类 bug **误判为满足**而漏掉。
**最容易漏的一种**：平台 API 调用藏在**传递依赖**里（不在你打包的 tgz 内）—— 静态扫描扫不到，只能装完再扫 `node_modules` 复核。

> ⚠️ **peer 范围是「前瞻声明」，不是「今天的陈述」。** 当你所依赖的平台即将进入新的版本线时，范围必须在升级**之前**放宽 —— 否则预检会拒掉一个**一个字都没改**的插件。放宽范围是**纯声明改动**（对当前实际运行的版本**零行为变化**），所以它**早做很便宜、晚想起来很贵**。

> 📌 **给平台建设者**：当平台自身是 prerelease 时，预检闸门若严格按默认语义判定，会产生大量假阳性 —— 抽样 300 个插件实测约 **39% 假阳性**，其中包含**已装且在正常跑**的插件。可行设计是「**先默认语义；不满足再用 prerelease 语义复核，能满足的只计数、不阻断**」。这个放宽改的是**闸门拦什么**，**不改包管理器自己的安装判定** —— 后者仍然是默认语义。

**症状**：装上去实例 `plugin tree failed to load` → **崩溃循环**（每次启动即崩，而平台的崩溃重启只止损、不禁用）。

### H6 · 重型依赖在入口静态 import

| 插件 | 加载后 `rss` 增量 |
|---|---|
| 某重型表格插件 | **+65.1 MiB** |
| 其间接依赖（嵌入式数据库） | +7.8 MiB |
| 平台自研轻量插件 ×3 | **0.0 MiB** |
| 某浏览器自动化库（**仅 import 主入口、未启动浏览器**） | **0.0 MiB** |

**结论**：**成本由「装了什么」决定，不是「装了几个」** —— 一个重型插件 ≈ 无穷多个轻量插件。
**做法**：原生绑定 / 引擎 / 浏览器这类重物，改 `await import()`，只在真正用到时加载。

---

### H7 · 客户端半边「静默挂死」：`inject` 依赖永不满足 🔴 **无报错、无日志**

**症状**：服务端一切正常（工具能调、数据真落盘），但**浏览器里什么都没有** —— 预览 UI 不出现，控制台无报错、日志也无痕迹。

**机理**：客户端插件的 `package.json` 里 `dsh.client.inject` 列了某个 **UI 包**，而该包在**这个角色的页面里根本不会下发**（例如被平台的角色补丁禁用）⇒ cordis 的 `inject waiting` **永不满足** ⇒ **客户端 fiber 永久挂起 ⇒ `apply()` 永不执行**。

**判据（通用排查手法）**：取实例页面 → 解析 `__DSH_BOOT__` → 把每行 `inject` 与**页面实际下发的客户端插件集合**求**差集**；
**差集非空 = 该客户端 fiber 永久挂起**。A/B 对照更直观：同一插件在不同角色下，缺的正是那几个"被禁的包"。

**修法**：从 `dsh.client.inject` 里**移除**在目标角色不必然存在的包。

> ⚠️ **铁律**：第三方客户端插件**不得把「官方 UI 包」写进 `dsh.client.inject`**，除非确认该包在**目标角色**里必然存在 ——
> 平台的角色补丁会禁用这些：`dsh-client-ui-settings-models` / `-settings-plugins` / `-settings-plugin-inventory` / `-cordis` / `dsh-client-hmr` / `dsh-host-directory-picker-auto`。
> **典型案例**：某插件写了 `@deepseek-ai/dsh-client-runtime` —— 该包在当前 dsh 版本**根本不存在** ⇒ 它的浏览器半边同样静默挂死。

> 📌 **使用者预期**：这类插件**不注册 `tool.*.toolview`** ⇒ **工具卡片不会变样**；能看内容的地方只有三处：会话**轮尾卡片**、输入框上方 **dock**、全屏审阅浮层。
> **旧回合不会回溯渲染** —— 硬刷新后要**新跑一轮**含该工具调用的回合。

### H8 · 原生绑定要求的 glibc 比宿主新

**症状 A —— 进程在**干完活之后**才死。**某个操作返回错误（`Collaboration HTTP request failed` / `ECONNRESET` / 健康检查超时），**但效果真的发生了** —— 数据在盘上、变更集已提交。**这个组合就是判据**：进程是在请求中途、在**做完事之后**被杀的。
**症状 B —— 某个操作把整个进程带崩，且是 `uncaught`（不是可降级的错误）**：`Error: … version 'GLIBC_2.35' not found`。

**为什么**：预编译的原生扩展（Rust / Node addon）按一个**最低 glibc** 链接；宿主更旧时 `require()` 直接抛错。而如果这个库是在**建投影**时被加载的，崩溃会把整个进程带走。
**⚠️ 不止一个进程会中招** —— ⛔ **别假设另一个进程是"只读、不建投影、不用打补丁"**（见 §4.6 ③）。**每一个**会加载该依赖的产物都要挂上修复，并且用**崩溃计数**验证，而不是读代码。

**上架前的检测** —— 拿扩展的要求和宿主的能力对一下：

```sh
ldd --version | head -1
objdump -T <binding>.node | grep -oE 'GLIBC_[0-9.]+' | sort -u | tail
```

**两条修法**

- **(a) 平台层**：换一个 glibc 足够新的基底镜像。这是基础设施决策，影响面覆盖**所有**租户。
- **(b) 插件层**：**自带所需运行时**，让受影响的进程经随包而来的加载器启动 ——
  `<runtime>/ld-linux-x86-64.so.2 --library-path <runtime>/lib:/usr/lib64:/lib64 <node> <entry>` ——
  目录**自探测** + 环境变量可覆盖 ⇒ **运行时不存在时行为与改动前逐字节一致**（零回归；**删目录即完全回退**）。

**⚠️ 自带 glibc 的两个坑**：
1. **必须整套一起给** —— 新 glibc 把 `libpthread` 并进了 `libc`，只给 `libc`/`libm` 却仍用宿主 `/lib64` 会撞 `GLIBC_PRIVATE` / `__libc_siglongjmp`；
2. **放在沙箱看得见的地方** —— 如果实例是把 `/usr` 只读挂进去的，`/usr/local` 可见，而很多别的路径不可见。

⚠️ **这种运行时不要随开源导出分发**（若是 LGPL 且只该留在服务器上）—— 它是**环境资产**，不是插件的一部分。

**上游可能本就有开关**：有些库提供关闭项（例如某个引擎配置，改成纯 JS 实现）。若有，就**判据化**为「原生绑定确实装不上才降级」，而不是写死 —— 这样宿主升级后**不改代码**就自动恢复快路径。

## 3. 改造六条规范（R-a ~ R-f）

> 满足这六条，插件就能在任意"单入口 + 多租户 + HTTPS"的托管平台上跑。

| # | 规范 | 具体做法 | 理由 |
|---|---|---|---|
| **R-a** | **对外入口唯一** | 浏览器可达面全部挂在 Host 的 webServer 上，用**同域相对路径**（如 `/myplugin-api/**`） | 复用平台反向代理 ⇒ **零新增暴露面** |
| **R-b** | **内部通信走 IPC** | 用 **unix domain socket** 或 **stdio**，**不用 TCP loopback** | 不经 IP 层 ⇒ 出网护栏管不到；天然**无端口冲突** |
| **R-c** | **不新增监听端口** | 需要多进程就 socket / stdio；确实需要 TCP 时**必须**绑定 `127.0.0.1` 且端口由 env 传入 | 多租户共主机下，端口 = 跨租户风险 + 漂移 |
| **R-d** | **重型依赖懒加载** | 原生绑定、引擎、浏览器、大型解析器改 `await import()` | 实测可从 +65 MiB 降到 0 MiB（未使用时） |
| **R-e** | **交付浏览器的地址一律相对** | 服务端只返回路径，客户端不拼绝对地址 | 绝对地址在托管平台必失效 |
| **R-f** | **出声失败，绝不静默降级** | 兼容垫片做不了事时，必须在**失败点**抛错或打日志；🚫 绝不 `return` 回"正常路径"悄悄走过。解析不到的依赖要显式报错，不是静默跳过 | 静默失效的垫片与**上游通用 bug** 长得一模一样：本案例为此多绕了几轮 —— 它自己的垫片从来没生效过（§4.6 ①） |

**兼容性（H5）单列为上线门槛**：`@deepseek-ai/*` 依赖范围用**默认语义**能接受平台版本；`import` 的符号在平台包里真实存在。

---

## 4. 实战：`dsh-univer-office` 的完整改造

> 📦 **本案例的改造件在这里** → [`examples/dsh-univer-office/`](examples/dsh-univer-office/)
> （改造补丁 + 新增模块 + 配套技能 + 上游基线与应用方法）
> ⚠️ **自带的 glibc 运行时刻意不在其中** —— 它是环境资产（见 §4.9）。
> 🙏 原插件由 **[dream-num](https://github.com/dream-num)** 开源（**Apache-2.0**）—— **感谢作者**。

### 4.1 症状背后的**两层独立阻碍**

该插件静态预检**全部通过**（依赖范围 ✅、导出符号 ✅），但平台上完全不可用。原因是两条**互相独立**的阻碍，**缺一都打不开**：

| 阻碍 | 机理 | 实测证据 |
|---|---|---|
| **① Host → Gateway** | gateway 由宿主 `spawn`，**继承同一 uid**；平台的出网护栏对实例 uid 段 → `127.0.0.0/8` 是 `reject with tcp reset` ⇒ **宿主连不上自己拉起的 gateway** | 创建文档恒报 `bundled Gateway did not become ready within 10000ms` |
| **② Browser → Viewer** | 源码**硬编码** `gateway = http://127.0.0.1:${port}`、`viewerUrl = ${gateway}/?file=…`，客户端拿它作 **iframe src** ⇒ 浏览器去**用户自己电脑**的 `127.0.0.1` 找 Viewer | `grep -o "viewerUrl: [^,]*" lib/index.js` |

**⇒ 哪怕把第 ① 条的 loopback 解封也没用**（第 ② 条拦在浏览器侧）。**只能改插件。**

> ⚠️ 预检通过**完全不代表**这两条背后没有**另外两层**阻碍 —— §4.5 的第 2、3 轮就是。**预检绿只说明"静态可查的部分没问题"，不说明"它能用"。**

### 4.2 为什么「换成服务器 IP」不行

这是最容易被想到、也最错的方案：

1. 出网护栏的封禁名单里**就包含服务器自身 IP** —— 换成它照样被拒；
2. 多租户同主机上**绑端口会跨租户泄露**；
3. HTTPS 页面嵌 HTTP iframe 会被**浏览器硬拦**。

**正解不是"换一个地址"，而是"换成一条路径"。**

### 4.3 改造清单（真实规模参考）

**新增模块**

| 模块 | 作用 |
|---|---|
| `shared/gateway-socket` | 解析 `UNIVER_DSH_GATEWAY_SOCKET`（路径值或 `auto`）+ 定义 `unix:<path>` 端点标识 |
| `shared/unix-http` | **HTTP over unix socket** 的最小客户端（`status` / `headers` / `text` / `json` / `arrayBuffer` / `signal`）—— **零新依赖** |
| `shared/gateway-request` | **统一传输入口** `requestGateway(endpoint, path, init)`：按 endpoint 前缀自动选 socket / TCP |
| `host/webServer/viewer-proxy` | **同源反向代理**：HTTP（流式，剥离 hop-by-hop 头）+ **WebSocket upgrade 转发** |
| `shared/viewer-paths` | 浏览器侧路径常量（独立于 webServer，避免反向依赖） |

**改动文件**：网关监听改支持 unix socket（**TCP 默认行为不变**）、launcher 把 socket 路径经 env 传给子进程、端点按传输打标（`http://127.0.0.1:<port>` 或 `unix:<path>`）、健康检查与探针支持 socket、客户端调用点统一改走 `requestGateway`、webServer 注册 viewer 代理与数据面路由、**`viewerUrl` 改成相对路径**。

### 4.4 三条关键设计决策（都是踩出来的）

**① socket 是「可选传输」，不是替换 TCP**

由 env 开启（设路径，或设 `auto` 取私有临时目录下的进程级路径）；**不设则完全保持上游原行为**。
好处：**对上游友好 —— 默认行为不变，可以安心合并**；回滚只需**删掉这个 env**。

**② 代理规则只有两条**

因为该插件的**所有数据面请求**都在同一个前缀下（含 WebSocket）：

```
/uf/**                 → 转发 gateway（HTTP + WS upgrade）
/<plugin>-api/viewer/** → 转发 gateway 的 / 与 /assets/**（页面 + 静态资源）
```

⚠️ **关键点**：Viewer 页面加载后会**用绝对路径**请求 `/uf/*`，所以代理**必须接管 `/uf/*`** —— 否则页面打开了，数据却拿不到（典型的"一半能跑"）。

**③ 两个平台侧机制坑（很隐蔽）**

| 坑 | 现象 | 解法 |
|---|---|---|
| **前缀匹配无「最长优先」** | 更短的 `/myplugin-api` 会**吃掉** `/myplugin-api/viewer/...`；单独注册会被 404 截胡 | viewer 代理必须放进**现有 router 的 dispatch 内**；不冲突的前缀才独立注册 |
| **WS 路由注册是精确路径** | 插件的 WS 路径含**动态段**（`/uf/<enc>[/worktrees/<id>]/…`）⇒ 无法一次性注册 | **按文件懒注册**：打开文件时种子化 + Map 去重 + 统一 dispose |

### 4.5 首轮改造之后还需要的六轮

> 首轮改造投放为 **0.2.15**。以下是其后的**六轮** —— 五轮由生产实测驱动（每修掉一层，下一层才露出来），最后一轮由平台升级驱动。
> 其中 **0.2.16 – 0.2.23** 这些构建都带着下表第 2 行那个静默垫片缺陷。

| # | 用户看到的现象 | 根因 | 怎么收口 | 投放 |
|---|---|---|---|---|
| **1** | Viewer 页面能打开但内容永远加载不出来；创建文档恒超时 | **两条独立阻碍** —— 出网护栏拒绝 host → gateway 的 loopback；交付浏览器的 `viewerUrl` 是硬编码绝对地址（§4.1） | unix socket 传输 + 同源 viewer 代理 + 相对路径 | 0.2.15 |
| **2** | 内容操作全线报**笼统的** `COLLABORATION_UNAVAILABLE`，而 `new` / `status` / `worktree` 都好 | **又两条互相独立的原因，且第二条只有修掉第一条才看得见**：① 插件**自己的 IPC 垫片静默失效**（§4.6 ①）；② 原生绑定要求 **glibc ≥ 2.35** 而宿主是 2.32 ⇒ 建投影时整个进程 `uncaught` 崩（§2 H8） | ① 把垫片安装**提到顶层 `await` 之前**，并让失败**出声**；② 加宿主兼容层，**仅在绑定确实装不上时**切纯 JS 引擎 | **0.2.24** |
| **3** | 写操作报 `Collaboration HTTP request failed`，**但数据真的落盘了** | **gateway 在提交变更集时也要建投影** ⇒ 撞同一个崩溃 ⇒ 网关进程在请求中途死 ⇒ 客户端读到连接重置 ⇒ **假错误**。这同时**推翻了**首轮"网关不需要打补丁"的判断（§4.6 ③） | 把宿主兼容层**同时挂到 gateway 构建**；修 CJS `import.meta` 陷阱（§4.6 ②） | **0.2.25** |
| **4** | 「能不能让 AI 直接给我生成 docx/xlsx？」—— 不行 | 不是缺陷：实例**没有任何 Office 库**。但**「没有库」≠「不能生成」** —— OOXML 就是 zip + XML，Python 标准库足够 | 随插件投放一个**零依赖**的 Office 生成技能（⚠️ 技能是**显式清单**注册，必须同步改清单 —— §4.6 ④） | **0.2.26** |
| **5** | 服务端完全健康，**浏览器里却什么都没有** —— 无报错、无日志 | `dsh.client.inject` 里写了被平台角色补丁禁用的 UI 包 ⇒ 客户端 fiber 永久等待（§2 H7） | 从 `inject` 里移除该包 | **0.2.27** |
| **6** | 外部 xlsx/docx **导入**、`.univer` **导出**成 Office，仍不可用 | 原生绑定按 **glibc ≥ 2.35** 链接，宿主是 2.32（§2 H8） | 自带一份 **glibc 2.35 运行时**（5 MB），两个进程都经它启动 —— **平台侧零改动**，删目录即回退 | **0.2.28** |
| **7** | *什么都没坏。* 是**平台自己**即将升级到新的 dsh 版本线 | 不是缺陷：插件在 `@deepseek-ai/dsh-*` 上的**七个** peer 范围锚定在当时的版本 ⇒ **下一个**平台版本会被判**不兼容**、插件被拒 | **升级之前先放宽声明**：给七个范围都追加新版本。纯声明改动 —— 对当前实际运行的版本**零行为变化** | **0.2.29** |

**这六轮给出的形状**：第 2–6 行**不是同一个 bug 反复发作**，而是**四类完全不同的故障**（打包器产物、系统库版本、进程生存期、依赖注入契约）**前后叠在一起**，每一层都要等前面那层被拆掉才看得见。第 7 行是**又一种**：**它不是缺陷，而是维护义务** —— 把兼容性声明写窄的插件，会在平台一动的那一刻被**正确地**拒掉；唯一避免方式是在平台动**之前**放宽声明。

### 4.6 四条各花掉一轮的陷阱

**① 打包器把插件自己的垫片静默废掉了**

插件在**模块作用域**安装 IPC 垫片（`fetch` / `http.request` / `WebSocket`），并在**顶层**调用 `await main()`。打包器把**模块级 `const` 降级为函数作用域 `var` 并提升**，于是 `main()` 跑起来时垫片读到的常量还是 `undefined` ⇒ `'http://unix/…'.startsWith(undefined)` **恒 false** ⇒ 垫片**悄悄把请求交回真正的 `fetch`**，后者去拨一个**字面名叫 `unix` 的主机**。

⇒ 唯一可见的症状是一个**笼统**错误，而且**从第一个构建起就受影响** —— 垫片逻辑本身是对的，是**打包顺序**把它废掉了。

**修法**：垫片安装提到顶层 `await` **之前**；origin 改用**函数内字面量**（双保险，防再被重排顺序废掉）；`http.request` 补认多种签名；**把「取不到 WS 包」改成出声报错** —— 它原先被静默吞掉，而正是这个"吞"掩盖了全部真相。

**② CJS 产物里 `import.meta` 是空对象**

在 **CJS** 构建里，esbuild 把 `import.meta` 降级成空对象（`var import_meta = {}`）⇒ `createRequire(import.meta.url)` 抛 `ERR_INVALID_ARG_VALUE` ⇒ **进程启动即崩**。
**修法**：`typeof __filename === 'string' ? __filename : import.meta.url`；并在 CJS 产物里把垫片自身的 import 显式指向该包的 **CJS 入口**（靠 `import.meta.url` 定位原生绑定的 ESM 入口在那里必崩）。
⇒ **两种产物都要跑。**「ESM 版能跑」**不能**证明 CJS 版没问题。

**③ 崩溃点不在你以为的地方**

首轮曾下结论「网关只读快照、不建投影、不用打补丁」。**这是错的**：网关在**提交变更集**时也要建 workbook 投影，于是**另一个进程**在**同一个原生绑定**上崩了 —— 而且因为它是**在请求途中**死的，客户端读到连接重置，于是对一个**已经成功**的操作报了失败。

⇒ **规矩**：只要涉及原生依赖，修复必须挂到**每一个**加载它的产物上，并且用**崩溃计数**验证，不要靠读代码。**"报错但数据在"就是"进程干完活之后才死"的签名。**

**④ 技能是「显式清单」注册的**

插件的技能**不是扫目录扫出来的**，而是列在宿主技能清单里（`src/host/skills/plugin.ts` 的 `DEFINITIONS`）。**只把技能目录丢进包里不会生效** —— 必须同时改清单并**重建** host 半边。

⇒ 与 H7 同族：**产物在，但没有任何东西把它接上**。凡是"某个能力没生效"，先问它**有没有被注册**，而不是先问文件在不在。

**顺带修掉的一个真缺陷**（由**严格解析器**抓出，不是由生成它的代码抓出）：生成的 OOXML 表格缺了**必需**子元素 `w:tblGrid` —— `python-docx` 会直接拒绝打开这种文件。**生成一种格式后，一定要用独立的严格解析器读回来。**

### 4.7 不碰生产也能定位「实例内」故障

> 适用于任何「**服务在跑，但内容操作报错**」的场景。全部在 `/tmp/<工作名>/` 里做 —— **不碰实例、不碰用户数据**。跑完清理。

1. **起一个自己的 gateway**（socket 模式），用**实装产物**，环境隔离：
   `env -i HOME=<tmp> PATH=/usr/local/bin:/usr/bin:/bin UNIVER_COLLAB_GATEWAY_SOCKET=<tmp>/gw.sock … setsid nohup node <pkg>/artifacts/gateway.cjs > <tmp>/gw.log 2>&1 &`
   起得来（`listening on unix:…`）且 `curl --unix-socket <sock> http://localhost/` 返回 **200** 且带页面标记 ⇒ **网关侧没问题**。
2. **手工构造 worker 请求**，以**租户 uid** 跑（`setpriv --reuid=<uid> --regid=<uid> --clear-groups`），载荷里给上 gateway 端点标识，`fileKey` 用 **`base64url(绝对路径)` 去掉 `=`**。
3. **看垫片轨迹**（打开插件的调试开关）：链路每一步都该出现（`fetch-called → fetch-result status=… / ws-construct / http-request host=…`）。**只出现第一行后面就断 ⇒ 垫片内部提前 return** —— 陷阱 ① 就是这么抓到的。
4. **兜底判据**：把 gateway 换成一个**什么都回 200 并打印请求的假 socket 服务器**。若假服务器**一个请求都没收到**而 worker 仍报错 ⇒ 故障在 **worker 侧**（请求根本没发出去）。
5. **清理**。另注意：**起实例本身就是流量的副作用** —— 某些形态下，任何带该实例主机名的请求都会把它拉起来（返回 401 也算）。

⚠️ 另外两个让排查变难的坑：临时目录里跑的 worker **解析不到被提升的依赖**（模块解析路径要指向包管理器的提升目录）；**环境变量给一半**会报 `Cannot find module`，看起来像"插件坏了"，其实只是路径缺了。

### 4.8 怎么验证（不碰生产）

1. **依赖与构建**：装依赖 → 构建（增量 ~5–16 s）；
2. **回归测试**：跑插件自带的集成冒烟（该插件 **18 项**：新建 / 状态 / 导入 / 导出 / 截图 / 打印 / 资源 / worktree 生命周期…）—— **证明改动没破坏原功能**；
3. **socket 实测**：把构建产物放到**临时隔离目录**（原生依赖用只读符号链接指向已有安装），启动后确认 `listening on unix:…sock`、`GET /` 返回 **200** 且页面标记存在、数据路由可达（返回 4xx 说明路由通、只是参数不对）；**同时跑一遍 TCP 对照**；
4. **清理临时产物**。

> ⚠️ **Windows 上验证不了 unix socket**（`AF_UNIX` 直接 `EACCES`，用纯 `node:net` 同样复现）—— 这是**平台限制，不是代码问题**。**必须在 Linux 上验**。

### 4.9 已知限制（诚实记录）

- **截图 / PDF（`compile_svg` / `lint` / `screenshot` / `print_pdf`）仍不可用** —— 而且这**不是 glibc 问题**：实例内**没有浏览器**，而 headless Chromium 要多占 **+200–400 MB**，撞实例内存配额。这属于**环境侧**决策（装浏览器，或接受这个缺口），插件侧修不了。
- **有一个依赖在上游只是 *devDependency*** —— 生产里能解析到，纯粹因为包管理器把它提升到了共享目录。这是**长期脆弱点**：一次干净的安装就可能让它消失。正解是把它正式声明出来。
- worktree 场景的 WS 只注册了**文件级**路径。
- 这是**fork**：上游更新需要合并（见 §7）。
- ⚠️ **自带的 glibc 运行时不在 `examples/` 的改造件里** —— 它是环境资产（LGPL、只该留在服务器上），此处**刻意不分发**。
- ✅ *对本文档初版的更正*：「worker 侧仍走 TCP」**已不再成立**。worker 的垫片正是第 2 轮（§4.5）修掉的那个缺陷；垫片一恢复，worker 同样经 socket 访问 gateway。

### 4.10 回滚

- **代码级**：不设那个 socket env ⇒ 立刻回到原 TCP 行为；
- **glibc 运行时**：**删掉运行时目录** ⇒ 所有进程回到朴素的 `<node> <entry>` 启动。不改代码、不重启平台；
- **平台级**：候选池把版本退回上游版本即可整体退场。

---

## 5. 平台侧要配合的另一半

插件改了，平台也得留好接口，否则白改：

| 平台能力 | 要求 |
|---|---|
| **env 透传** | 平台 → 实例 → 插件子进程，能把「socket 路径」「运行时目录」这类配置传下去 |
| **同源反代** | 把插件的路径前缀转发到实例内服务，且**支持 WebSocket upgrade**、正确设置 `Host` / `X-Forwarded-*`、**不缓冲**流式响应 |
| **内存预算** | 给每个插件**标注加载成本**（实测 `rss` 增量），并在启用前做**预估与拦截**（超单实例上限时要求确认） |
| **兼容性预检** | 导入即判定「依赖范围 + 导出符号」，不兼容**默认拒绝**并把逐条依据回显给管理员。⚠️ 应**先按默认语义判，再用 prerelease 语义复核，能满足的只计数、不阻断** —— 在 prerelease 平台上从严会产生大量假阳性（§2 H5） |
| **启停安全** | 启用后**探活** + 失败**快照回滚** + 逐插件**隔离标记**（把不兼容插件单独禁掉，而不是拖垮整个实例） |
| **把「托管友好性」与「资源成本」列入评估维度** | 「兼容」≠「可用」：插件评审标准里除了依赖解析，还要有**托管友好性**（一切对外交互是否都走唯一入口？）与**资源成本** |
| **给自带运行时留位置** | 若允许插件自带运行时（更新的 glibc、JDK、浏览器），要留一条沙箱**看得见**的路径 —— `/usr` 只读挂入时，`/usr/local` 可见，很多别的路径不可见 |
| **隔离模型的边界** | 出网护栏（拒实例主动连 loopback / 元数据端点）、`/etc` 白名单、只读的平台策略文件 —— 插件不能假设这些不存在 |

### 5.1 本轮投放暴露出的平台侧缺陷

「安全地投放一个插件」既是插件的事，也同样是平台的事：

| 缺陷 | 为什么要紧 | 修法 |
|---|---|---|
| `async` 的 `uninstall(...)` 被**同步 `try/catch` 里不带 `await`** 地调用 | rejection 逃出 `catch` ⇒ **未捕获拒绝 ⇒ 整个平台进程退出** —— 全体租户瞬断、所有实例被停，且出事的 profile 停在**半应用态**（该实例此后每次启动即崩） | 补 `await`；路由最外层补 `.catch()`，让同类遗漏再也不会崩掉进程 |
| 卸载路径传了一个**只在安装子命令上存在**的包管理器 flag | 禁用路径**必然失败** ⇒ 必然走到上面那条 | 用卸载子命令真正接受的 flag；⚠️ 安装侧那个 flag 是合法的，**不要一起改** |
| **`apply` 对「已启用」插件是 `noop`** | 无法一步把已在跑的插件升到新版本：平台报成功，实际什么都没变 | 把「已启用项」也算作目标，或把「更新 = 先停用再启用」明确下来 —— 并**写进文档**，否则每个运维都会踩一次 |
| 任务**终态是 `success` 而不是 `done`** | 任何等 `done` 的轮询都会空转 | 统一一个名字 |
| 探活失败可能是**容量**问题，不是兼容性问题 | 实例堆装不下插件集合时，平台会走「隔离定位」分支 —— 上面那两个缺陷正是由此触发 | 配额**按插件集合动态计算**（别写死），并在错误信息里说出来 |

⚠️ 由此得出的纪律：**启停插件绝不该有能力把平台带崩。** 要做的是**兜住它** —— 逐插件隔离、失败回滚快照、有界重试 —— 而不是指望每个调用点都写对。

---

## 6. 上架前自查清单

```
[ ] 浏览器侧不存在绝对 URL（client 产物内 grep `http://` / `https://` 字面量）
[ ] 交付浏览器的资源地址全部由 Host 生成、且为相对路径
[ ] 内部进程间通信走 unix socket / stdio，不用 TCP loopback
[ ] 没有新增监听端口（确有需要时绑 127.0.0.1 且端口由 env 传入）
[ ] 重型依赖（原生绑定 / 引擎 / 浏览器）已改 await import() 懒加载
[ ] @deepseek-ai/* 依赖范围在「默认语义」下能接受平台内置版本
[ ] ……并且也能接受**下一个**平台版本 —— 在平台动**之前**放宽声明，而不是之后
    （纯声明改动，早做很便宜）
[ ] import 的平台符号在平台包里有运行时导出（含传递依赖那一层）
[ ] 实测加载内存增量（>30 MiB 标注 / >60 MiB 需谨慎）
[ ] 原生绑定在宿主上原样装得上 —— 否则自带运行时并经它启动，
    且修复挂到「每一个」加载该依赖的进程上（不只是你先发现的那一个）
[ ] dsh.client.inject 里没有「目标角色不必然存在」的 UI 包
[ ] 每个兼容垫片都「被证明真的生效」（静默退回正常路径的垫片比没有垫片更糟）
[ ] 没有错误路径被静默吞掉：解析不到的依赖要出声失败
[ ] 新增技能 / 资源时同步改了它的「显式清单」，不只是把文件放进包里
[ ] 两种产物（ESM 与 CJS）都跑过 —— CJS 产物里 import.meta 是空对象
[ ] 每一种生成的格式都用独立的严格解析器读回来过
[ ] 自带回归测试能跑通，且改动前后结果一致
[ ] 有能力在「不设插件专用 env」时回退到原行为
```

---

## 7. 更优路径：把改造提给上游

fork 是**长期成本**（谁维护 / 上游更新怎么合）。更好的做法是把改造作为 **issue + patch** 提给上游。

**论证口径很重要**：不要说「请支持我们的平台」，而要说 ——

> **「你的架构在容器化 / 托管 / 多租户环境里可以更简单、更安全：把 loopback TCP 换成 unix socket + 同源相对路径，能去掉对 IP 与端口的假设。」**

这样对上游是**纯粹的技术收益**（更少假设、更少端口、更安全），即使他们没有托管场景也会愿意接受。

> ⚠️ **要挑「提什么」**。unix socket + 相对路径那条对任何人都是实打实的简化；**自带 glibc 运行时不是** —— 它只是针对某一台宿主基底镜像的绕过，而**本就提供纯 JS 回退的库，正确的做法是配置它去用回退**（§2 H8）。把环境绕过提给上游，会被拒，而且被拒得有理。

---

## 附录 · 快速自检命令

```sh
# 1) client 产物里有没有绝对 URL（H2，阻断级）
grep -rInoE "https?://[A-Za-z0-9._:/-]+" lib/ dist/ | grep -v "://127\.0\.0\.1" || true
grep -rIn  "127\.0\.0\.1:[0-9]" lib/ dist/ || true

# 2) 有没有自建监听（H3）
grep -rInE "\.listen\(|createServer\(" lib/ dist/ | head

# 3) 声明了哪些平台包（H5 判据 A）
node -e "const p=require('./package.json');console.log(p.dependencies,p.peerDependencies)"

# 4) 加载内存（H6，在**隔离 cgroup** 里测，别在生产实例上测）
node --expose-gc -e "const m0=process.memoryUsage().rss;import('./lib/index.js').then(()=>{global.gc();console.log('rss +' + ((process.memoryUsage().rss-m0)/1048576).toFixed(1) + ' MiB')})"

# 5) 原生绑定 vs 宿主 glibc（H8）
ldd --version | head -1
objdump -T <binding>.node | grep -oE 'GLIBC_[0-9.]+' | sort -u | tail

# 6) CJS 产物有没有把 import.meta 抹掉？（H8 / §4.6 ②）
grep -rn "import_meta = {}" lib/cjs/ 2>/dev/null | head   # ⇒ 依赖它的代码必抛
```

---

**相关**：平台的功能一览与部署方式见 [`README.md`](README.md)。
